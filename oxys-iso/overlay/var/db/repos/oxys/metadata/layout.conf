# Oxys first-party ebuild overlay. Depends on the gentoo repo for eclasses
# (meson, git-r3, xdg, ...), so it declares gentoo as its master.
masters = gentoo
thin-manifests = true
sign-manifests = false
