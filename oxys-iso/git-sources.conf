# Live Git sources that must be available before catalyst starts.
#
# Fields (whitespace-separated):
#   <package-atom> <repository-uri> [ref]
#
# The helper reproduces git-r3's store-name algorithm. Add one row here for each
# live ebuild that the ISO package set actually uses.
gui-shells/noctalia https://github.com/noctalia-dev/noctalia.git HEAD
